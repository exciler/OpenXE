doctrine:
  dbal:
    url: '%env(resolve:DATABASE_URL)%'
    schema_filter: ~^user~

  orm:
    mappings:
      OpenXE:
        type: attribute
        is_bundle: false
        dir: '%kernel.project_dir%/src/Entity'
        prefix: 'OpenXE\Entity'
        alias: OpenXE