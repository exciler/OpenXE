services:
  _defaults:
    autowire: true
    autoconfigure: true

  OpenXE\:
    resource: '../src/'

  Config:
    class: Config
    factory: ['Xentral\Core\LegacyConfig\ConfigLoader', 'load']

  erpooSystem:
    class: erpooSystem
    arguments:
      $serviceContainer: '@service_container'
    properties:
      Tpl: '@TemplateParser'

  Session:
    class: Session

  Player:
    class: Player

  TemplateParser:
    class: TemplateParser

  twigpublic:
    alias: twig
    public: true